[package]
name = "gitenv"
version = "0.1.0"
edition = "2024"

[lib]
name = "gitenv"
path = "src/lib.rs"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"

[dev-dependencies]
tempfile = "3"
